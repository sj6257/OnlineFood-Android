<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
     >



        <ExpandableListView
            android:id="@+id/expandablelist_menu"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"         
            android:background="#eeeeee"
            android:paddingBottom="4dp"
            android:paddingTop="4dp" 
            android:layout_above="@id/layout_selector"
            >
        </ExpandableListView>
 

    <LinearLayout
        android:id="@+id/layout_selector"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@android:color/transparent"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal" >

        <!-- layout_gravity: means how that element will be placed in its parent -->
        <!-- gravity:how the details of element ie text,etc will be place in element itself -->

        <Button
            android:id="@+id/btn_EmptyCart"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginLeft="4dp"
            android:layout_marginRight="2dp"
            android:layout_weight="1"
            android:background="@drawable/btn_background1"
            android:gravity="center"
            android:maxWidth="@dimen/MaxButtonSize"
            android:minHeight="40dip"
            android:text="@string/btn_EmptyCart"
            android:textColor="@drawable/btn_color1" />

        <Button
            android:id="@+id/btn_MyCart"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginLeft="2dp"
            android:layout_marginRight="4dp"
            android:layout_weight="1"
            android:background="@drawable/btn_background1"
            android:gravity="center"
            android:maxWidth="@dimen/MaxButtonSize"
            android:minHeight="40dip"
            android:text="@string/btn_MyCart"
            android:textColor="@drawable/btn_color1" />
    </LinearLayout>

</RelativeLayout>